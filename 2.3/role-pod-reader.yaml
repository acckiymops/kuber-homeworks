apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: developer
  namespace: default
rules:
- apiGroups: [""]
  resources:
    - pods
    - pods/log
  verbs: # РАЗРЕШЕННЫЕ ДЕЙСТВИЯ:
    - get
    - list
